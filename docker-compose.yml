version: '3.8'

services:
  secure-gateway:
    build: .
    ports:
      - "38291:8000" # Запуск на нестандартном внешнем порту (например, 38291)
    environment:
      - SECRET_ROUTE_PATH=21d85daf-1b8f-4e59-af4f-87fdf84afa90
    restart: always